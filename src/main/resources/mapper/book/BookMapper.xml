<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hippobookproject.mapper.book.BookMapper">
<!--  <select id="CategoriePage">
      SELECT * FROM TBL_BOOK
  </select>
    <insert id="insertCategoriePage">
        INSERT INTO TBL_BOOK (BOOK_ID, BOOK_NAME, BOOK_WRITER, BOOK_DATE, PUBLISHER_ID) VALUES (#{id},#{name},#{writer},#{date},#{id})
    </insert>
    <update id="updateCategoriePage">
        UPDATE TBL_BOOK SET BOOK_ID = #{id} WHERE BOOK_ID = #{id}
    </update>
    <delete id="deleteCategoriePage">
        DELETE FROM TBL_BOOK WHERE BOOK_ID = #{id}
    </delete>-->
</mapper>